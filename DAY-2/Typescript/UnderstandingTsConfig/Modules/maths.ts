export function Add(x:number, y:number):number {
    return x + y;
}

export default function Multiply(x:number, y:number):number {
    return x * y;
}

export function Divide(x:number, y:number):number {
    return x / y;
}

//File can have only default export and multiple named exports

// for  default export we can use any name while importing irrespective of function name

//enums